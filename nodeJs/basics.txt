1.it is a server-side language
2.it allows a website or an App to access the remote/pc directly by the app.
3. it is open source javascript run time environment 
4. some of the websites example are trello,ebay,Netflix etc.
REPL = READ EVALUATION PRINT LOOP
this allows us to execute the code in bite sized chuncks
the is like a environment where we can write and execute js code directly 

to entering into repl => node +enter
to exit of repl=> .exit or ctrl+c (x2 times)

Node Documentation=>https://nodejs.org/dist/latest-v20.x/docs/api/

NPM package Manager 
